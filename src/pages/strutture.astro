---
// Pagina del vecchio sito personale (gli ambulatori di un singolo
// professionista): fuori dal modello piattaforma — redirect permanente.
export const prerender = false;
return Astro.redirect("/cerca", 301);
---
